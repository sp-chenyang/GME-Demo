== 如何安装 ==

1. 解压到任何位置，比如
{{{
D:\glue
}}}
2. 运行register.bat进行注册
3. 重启max（如果之前已经在运行）
4. 打开max，将如下mcr文件拖放到max中（这样来安装max插件）
{{{
glue.zip\glue\gutil\client\max\max_gmeditor.mcr
}}}

== 如何渲染 ==

1. 打开一个max文件，比如`scene.max`
2. 点击菜单栏的Glue -> Render
3. 等待。
